<div class="max-w-md p-6 mx-auto border rounded-md shadow-lg border-gray-500/20 backdrop-blur-lg bg-gray-50/10">

  <%= content_for(:title, t('.title')) %>

  <div class="text-[#004681] text-center my-5">入力されたメールアドレスにパスワードリセット用のメールを送信します</div>
  <!-- パスワードリセットリクエスト画面の入力フォームを配置 -->
  <!-- 
    <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
    <!-- メールアドレス 
    <div class="mb-5 field">
      <%= f.label :email, class:"form-label my-2" %>
      <%= f.email_field :email, class:"form-input", autofocus: true, autocomplete: "email" %>
    </div>
    <!-- パスワードリセットリクエストボタン 
    <div class="my-5 actions btn-secondary">
      <%= f.submit t("devise.passwords.new.send_me_reset_password_instructions") %>
    </div>
  <% end %>
  -->
  実装まだ
  <span class="text-sm font-medium text-left text-gray-700">ブラウザバックで戻る</span>
</div>
